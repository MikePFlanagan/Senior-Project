<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MusicAcademyCRM.Students"
             BackgroundColor="Maroon">

    <StackLayout VerticalOptions="Center">
        <ListView x:Name="studentListView"
              BackgroundColor="{StaticResource canvasbackgroundColor}"
              ItemSelected="StudentListView_OnItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>

                    <TextCell Text="{Binding Name}"
                          TextColor="{StaticResource textColor}"
                          StyleId="disclosure"
                          Detail="{Binding Instruments}"
                          DetailColor="{StaticResource detailColor}" />
                </DataTemplate>

            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>