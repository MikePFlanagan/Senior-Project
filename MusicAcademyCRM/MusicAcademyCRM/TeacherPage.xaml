<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MusicAcademyCRM.Teachers"
             BackgroundColor="{StaticResource maroonColor}">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center">
            <ListView x:Name="teacherListView"
                      BackgroundColor="{StaticResource canvasbackgroundColor}"
                      ItemSelected="TeacherListView_OnItemSelected">
                <ListView.ItemTemplate>                    
                    <DataTemplate>
                        <TextCell Text="{Binding Name}"
                         
                        TextColor="{StaticResource textColor}"
                          StyleId="disclosure"
                          Detail="{Binding Instruments}"
                          DetailColor="{StaticResource detailColor}"/>
                    </DataTemplate>

                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>