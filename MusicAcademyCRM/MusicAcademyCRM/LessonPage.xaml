<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MusicAcademyCRM.Lessons"
             BackgroundColor="Maroon">


    <ListView x:Name="lessonListView"
              BackgroundColor="{StaticResource blackColor}"
              ItemSelected="lessonListView_OnItemSelected">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Instrument, StringFormat= '{0} lesson on'}"
                          TextColor="White"
                          StyleId="disclosure"
                          Detail="{Binding StartDate, StringFormat='{}{0:dddd a\\t h:mm tt,         o\\n MMMM dd yyyy}'}"
                          DetailColor="{StaticResource maroonColor}" />                    
                          
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>